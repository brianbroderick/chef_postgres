standby_mode = 'on'
primary_conninfo = 'user=<%= @config[:repl][:username] %> password=<%= @config[:repl][:password] %> host=<%= @config[:repl][:hostname] %> port=5432 sslmode=prefer sslcompression=1 krbsrvname=postgres'
