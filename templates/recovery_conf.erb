standby_mode = <% if @config[:standby] == "hot_standby" %>'on'<% else %>'off'<% end %>
primary_conninfo = 'user=<%= @config[:username] %> password=<%= @config[:password] %> host=<%= @config[:hostname] %> port=5432 sslmode=prefer sslcompression=1 krbsrvname=postgres'
