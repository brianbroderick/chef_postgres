PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games"
<% @environment.each do |key, value| %>
<%= "#{key}=\"#{value}\"" %>
<% end unless @environment.empty? %>
CHEF_UPDATE_ENV="true"